{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ItemMessage edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('item_show',{ 'slug' : app.request.get('slug'),'id' : app.request.get('item_id') }) }}">
            Back to the item
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
