{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Item creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('item',{'slug' : app.request.get('slug')})}}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}